### Current State of the World

The server has two buttons. "Update Data" shows this error when I click on it from my webbrowser: "Import error (macnotesapp not available on Linux?): macnotesapp is not available on this platform (macOS only)"

### Ask

I want the server to autodetect the device/platform and disable the "Update Data" button (with a hover tooltip) that says why. Basically we cannot update data yet. By "yet" I mean because I think we might be able to rework the API later with proper iOS and mac authentication to hit the notes app from the server, but let's not do that yet. Just disable with a note like "coming soon..."
